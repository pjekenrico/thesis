
\usepackage[square,sort&compress,sectionbib,numbers]{natbib}
\usepackage{chapterbib}
\usepackage[english]{babel}
\usepackage[babel]{csquotes}
\usepackage[utf8]{inputenc}
\usepackage{siunitx}
\sisetup{per-mode=symbol-or-fraction, load-configurations=abbr}
\setcounter{tocdepth}{2}
\usepackage{graphicx,subfigure,subfigmat}

\usepackage[format=plain,justification=justified,font={small,it}]{caption}
\DeclareCaptionFormat{custom}
{%
    \textbf{#1#2}\textit{\small #3}
}
\captionsetup{format=custom}
\usepackage{enumitem}
\usepackage{amsthm}
\usepackage{longtable}
\usepackage{appendix}
\usepackage{minitoc}
\usepackage{latexsym}
\usepackage{amsmath,amssymb,amsfonts,nicefrac,bm}
\usepackage{algorithm}
\usepackage{algpseudocode}
\usepackage{texdraw,tabularx,supertabular,psfrag,multirow}
\usepackage[table,dvipsnames]{xcolor}
\usepackage{notoccite}
\usepackage[european]{circuitikz}
\usepackage{moreverb}
\usepackage{siunitx}
\sisetup{per-mode=symbol-or-fraction, load-configurations=abbr}
\usepackage{calligra}
\usepackage[export]{adjustbox}
\usepackage{mathrsfs}
\usepackage{pdfpages}
\usepackage{pgfplots}
\usepackage{pgfplotstable, booktabs}
\usepackage{tikz}
\newtheorem{theorem}{Theorem}
\DeclareMathOperator{\tr}{tr}

\usepackage[pdftex,pdfborder={0 0 0},
	colorlinks=true,
	linkcolor=black,
	citecolor=black,
	pagebackref=false,
]{hyperref}	% Créera automatiquement les liens internes au PDF

\usepackage{tocbibind}
\usepackage{geometry}
\usepackage{fancyhdr}
\usepackage{parskip}

\usepgfplotslibrary{fillbetween}
\usetikzlibrary{patterns,shapes,snakes,positioning,calc}
\usepgfplotslibrary{external}
\usetikzlibrary{arrows,positioning}
\tikzset{
	>=stealth',
	punkt/.style={
			rectangle,
			rounded corners,
			draw=black, very thick,
			text width=6.5em,
			minimum height=2em,
			text centered},
	pil/.style={
			->,
			thick,
			shorten <=2pt,
			shorten >=2pt,}
}

\hbadness=10000
\vbadness=10000
\let\orgautoref\autoref\renewcommand{\autoref}[1]{\def\equationautorefname{Eq.}\def\figureautorefname{Fig.}\orgautoref{#1}}
\definecolor{red}{HTML}{aa0000}
\definecolor{blue}{HTML}{00007f}
\definecolor{green}{HTML}{005500}
\usepackage{lineno}
\usepackage{tcolorbox}
\tcbset{
    abstract/.style={
        colback=blue!20!white, % Background color
        colframe=blue!20!black, % Frame color
        left=2mm, right=2mm, top=2mm, bottom=2mm, % Padding
        boxrule=0.5mm, % Box border width
        width=\textwidth, % Box width,
        fontlower=\itshape,
        fontupper=\itshape,
        before lower=\textit{\textbf{Resumé}}\vspace*{0.25cm},
        before upper=\textit{\textbf{Abstract}}\vspace*{0.25cm}, % Space before the title
    }
}
\tcbset{
    conclusions/.style={
        colback=blue!20!white, % Background color
        colframe=blue!20!black, % Frame color
        left=2mm, right=2mm, top=2mm, bottom=2mm, % Padding
        boxrule=0.5mm, % Box border width
        width=\textwidth, % Box width,
        fontlower=\itshape,
        fontupper=\itshape,
        before upper=\textit{\textbf{Conclusions}}\vspace*{0.25cm}, % Space before the title
    }
}